#6 Add the dimensions to the Qlik Sense table properties
foreach ($dimension in $dimensions) {
    $newDimension = [PSCustomObject] @{
        qDef = @{
            qFieldDefs = @($dimension.qFieldDefs)
            qFieldLabels = @($dimension.qLabelExpression)
        }
    }
    $newTableProperties.qHyperCubeDef.qDimensions += $newDimension
}
